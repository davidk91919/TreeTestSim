% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_test_sims.R
\name{assign_alt}
\alias{assign_alt}
\title{Assign Null/Alternative Status in a k-ary Tree}
\usage{
assign_alt(t, tree_info)
}
\arguments{
\item{t}{Numeric in \eqn{[0, 1]}; the probability that a leaf is non-null.}

\item{k}{Integer; the branching factor.}

\item{l}{Integer; the depth of the tree (0-based).}

\item{level_offsets}{Integer vector, part of the output from \code{\link{get_level_info}}.}

\item{level_sizes}{Integer vector, part of the output from \code{\link{get_level_info}}.}
}
\value{
A logical vector of length \code{sum(level_sizes)}, indicating for each node
whether it is alternative (\code{TRUE}) or null (\code{FALSE}).
}
\description{
Randomly marks each leaf node as alternative (non-null) with probability
\code{t}, then propagates that status upward so that all ancestors of a non-null leaf
become non-null as well.
}
\examples{
info <- get_level_info(k = 2, l = 2)
alt_vec <- assign_alt(
  k = 2, l = 2, t = 0.5,
  level_offsets = info$level_offsets, level_sizes = info$level_sizes
)
alt_vec

}
