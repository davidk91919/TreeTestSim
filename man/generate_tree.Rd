% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treeSimFWER.R
\name{generate_tree}
\alias{generate_tree}
\title{Generate a k-ary Tree with Treatment Effects}
\usage{
generate_tree(level, max_level, k, t)
}
\arguments{
\item{level}{An integer specifying the current level in the tree (starting at 0).}

\item{max_level}{An integer specifying the maximum depth of the tree.}

\item{k}{An integer specifying the number of children per node.}

\item{t}{A numeric value in \link{0,1} giving the probability that a leaf is non-null (has a treatment effect).}
}
\value{
A list representing the tree with components \code{level}, \code{nonnull}, and \code{children}.
}
\description{
Recursively generate a k-ary tree with levels 0 to \code{max_level}. At the leaves,
each node is assigned a treatment effect (\code{nonnull} flag) with probability \code{t}.
For internal nodes, the \code{nonnull} flag is set to \code{TRUE} if any descendant is non-null.
}
\examples{
tree <- generate_tree(0, max_level = 3, k = 3, t = 0.2)
print(tree)

}
