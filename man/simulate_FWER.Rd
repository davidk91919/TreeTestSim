% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/treeSimFWER.R
\name{simulate_FWER}
\alias{simulate_FWER}
\title{Simulate the Family-Wise Error Rate (FWER)}
\usage{
simulate_FWER(n_sim, t, k, max_level, alpha, N_total, beta_base = 0.1)
}
\arguments{
\item{n_sim}{An integer giving the number of simulation runs.}

\item{t}{A numeric value in \link{0,1} for the probability that a leaf is non-null (has a treatment effect).}

\item{k}{An integer specifying the branching factor (number of children per node).}

\item{max_level}{An integer for the maximum depth of the tree (levels 0 to \code{max_level}).}

\item{alpha}{A numeric value for the significance level.}

\item{N_total}{A numeric value for the total sample size available at the root.}

\item{beta_base}{A numeric value for the base parameter of the beta distribution at the root (default is 0.1).}
}
\value{
A numeric value between 0 and 1 representing the estimated FWER.
}
\description{
Run the hierarchical testing procedure \code{n_sim} times with a given configuration and
estimate the family-wise error rate (FWER), defined as the proportion of runs in which a type I error occurred.
}
\examples{
fwer <- simulate_FWER(n_sim = 10000, t = 0.2, k = 3, max_level = 3, alpha = 0.05, N_total = 1000)
print(fwer)

}
